#include "100-main.h"
/**
 * takes two integers or floates 
 * return the sum, differece, product, quotient or remainder of the two numbers
 */
int add(int a, int b)
{ 
 return (a+b);
}

int sub(int a, int b)
{
 return (a-b);
}

int mul(int a, int b)
{
 return (a*b);
}

int div(int a, int b)
{
 return (a/b);
}

int mod(int a, int b)
{
  return (a%b);
}
